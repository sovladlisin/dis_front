import * as React from 'react';

interface IDDDProps {
}

const DDD: React.FunctionComponent<IDDDProps> = (props) => {
    return <><button onClick={() => { alert('ddddd') }}>ddddddd</button></>;
};

export default DDD;
